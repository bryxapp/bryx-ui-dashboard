import { Typography } from 'antd';

const Loading = () => {
    return (
        <div style={{ textAlign: 'center' }}>
            <Typography.Title level={4}>
                Loading...
            </Typography.Title>
        </div>
    );
}

export default Loading;